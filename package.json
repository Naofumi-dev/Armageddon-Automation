{
  "name": "armageddon-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website",
  "scripts": {
    "build": "mkdir -p dist && cp -R * dist/ 2>/dev/null || : && sed -i \"s|__API_KEY__|$API_KEY|g\" dist/index.html"
  }
}